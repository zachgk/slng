Type Data
    x: Set Number
    y: ???
    # len(x) == 5

data = Set Data
unknowns = Set Data

File CSV Input regression.in
    [d In data]
        d.x, d.y

File CSV Input unknowns.in
    [d In unknowns]
        d.x

File CSV Output regression.out
    [d In unknowns]
        d.y
