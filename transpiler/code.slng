Type Student
    grades: Set Number
    count: len(grades)
    gpa: sum(grades)/len(grades)

File Text Input input.in
    [s In students]
        [s.grades]
        "-1"

students = Set Student()

Output
    [s In students]
        sum(s.gpa)

